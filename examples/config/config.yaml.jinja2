{% set deployment_name = "demo" %}
parameters:
  deployment:
    name: "{{ deployment_name }}"
    dns: "{{ deployment_name }}.example.local"

_taskcask_directives:
  load_next:
  - config.main.yaml.jinja2
